[build-system]
requires = [
  "meson-python>=0.15",
  "numpy>=2.0"
]
build-backend = "meson_python"

[project]
name = "outflowpy"
version = "0.0.1"
description = "Outflow field modelling"
requires-python = ">=3.9"
dependencies = ["numpy"]

[tool.meson-python]
module-name = "outflowpy"
